# Changelog

## 0.1.0

- Introduced `redaction-kit` as the shared observability and audit redaction boundary.
- Added JSON key, JSON pointer, regex, URL query parameter, Prometheus label, and stable sampling helpers.
