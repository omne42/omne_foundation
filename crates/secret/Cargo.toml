[package]
name = "secret"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"
error = { path = "../error" }
serde_json = "1"
tokio = { version = "1", features = ["fs", "io-util", "macros", "process", "rt", "rt-multi-thread", "sync", "time"] }

[dev-dependencies]
tempfile = "3"
