# error-kit-protocol

这个文件保留为历史说明。

当前活跃 crate 已改名并收敛为：

- [`structured-text-protocol`](./structured-text-protocol.md)

对应源码目录也已经改为 `crates/structured-text-protocol/`。如果你在旧提交、旧文档或旧讨论里看到 `error-kit-protocol`，请把它理解为迁移前名字。
